{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.2"
  },
  "type": "module",
  "dependencies": {
    "firebase": "^11.6.0",
    "wasm-pack": "^0.13.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
